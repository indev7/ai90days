# Database Migration Guide

SQLite support has been removed. The application now uses PostgreSQL only.

If you need the legacy SQLite→PostgreSQL migration steps or scripts, refer to the repository history before SQLite removal.
